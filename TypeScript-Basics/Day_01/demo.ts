//TypeScript is a statically typed language, which means that you can specify the type of variables, function parameters, and object properties.

let n1: number = 1;
let n2: number = 2;

function add(n1,n2 ){
    console.log(n1 + n2);
}

add(1, 2);